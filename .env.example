# Copy this file to .env and fill in real values.
# Never commit your real secrets. The .env file is ignored by git.

# OpenAI provider (used if no key is typed into the UI)
OPENAI_API_KEY=sk-REPLACE_ME

# Internal provider credentials (optional unless using provider 'internal')
INTERNAL_ACCESS_TOKEN=replace_me_token
INTERNAL_CHAT_URL=https://internal-chat-endpoint.example.com

# Optional timeout overrides (seconds)
# OPENAI_TIMEOUT=60
# INTERNAL_TIMEOUT=60
